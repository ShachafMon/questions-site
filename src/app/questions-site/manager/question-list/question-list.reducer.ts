import { Action } from "@ngrx/store";
import { HttpService } from "../../Services/http.service";

const initialState = {

};


export function questionListReducer(state, action : Action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }

}